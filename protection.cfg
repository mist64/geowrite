MEMORY {
	LINK:   start = $0000, size = $0002;
	ENTRY:  start = $0001, size = $0005;
	DISK:   start = $0705, size = $0075;
	MAIN:   start = $807A, size = $0086;
}

SEGMENTS {
	LINK:   load = LINK,   type = rw;
	ENTRY:  load = ENTRY,  type = rw;
	DISK:   load = DISK,   type = rw;
	MAIN:   load = MAIN,   type = rw;
}
